// src/components/Header.js
import React from 'react';
import '../styles/header.css'; // We will create this CSS file later

const Header = () => {
  return (
    <header className="header">
      <h1>Student Management Dashboard</h1>
    </header>
  );
};

export default Header;